services:
  app:
    build: .
    restart: unless-stopped
    ports:
      # только localhost — наружу отдаёт nginx (80/443)
      - "127.0.0.1:3001:3001"
    env_file: .env
    volumes:
      - db:/app/db
      - uploads:/app/uploads

volumes:
  db:
  uploads:
