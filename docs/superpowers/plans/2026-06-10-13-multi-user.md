# Multi-User Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Несколько пользователей: вход через выбор профиля + пароль, создание профиля по коду семьи, личные колоды/прогресс/стрик, общие встроенные колоды.

**Architecture:** По спеке `docs/superpowers/specs/2026-06-10-multi-user-design.md`. Миграция 004 добавляет `users`, `decks.user_id` (NULL = встроенная) и пересоздаёт `progress`/`study_days` с `user_id` (старые данные удаляются — прода нет). Пароли — scrypt из `node:crypto`. JWT несёт `sub = user.id`, middleware кладёт `userId` в `req`, все репозитории принимают `userId`. Фронт: экран выбора профилей вместо парольного входа.

**Tech Stack:** Express + better-sqlite3 + jsonwebtoken, React 18 + TS, vitest.

---

## File Structure

```
server/src/
  db/migrations/004_users.sql        # НОВЫЙ
  auth/password.ts                   # НОВЫЙ: hashPassword, verifyPassword (scrypt)
  auth/users.repository.ts           # НОВЫЙ: createUser, getUserByName, getUser, listUsers
  auth/auth.service.ts               # МОДИФИЦ.: signToken(userId), verifyToken → userId|null
  auth/auth.routes.ts                # МОДИФИЦ.: GET /users, POST /register, POST /login
  auth/auth.middleware.ts            # МОДИФИЦ.: req.userId, проверка существования юзера
  decks/decks.repository.ts          # МОДИФИЦ.: listDecks/createDeck с userId
  decks/deckStats.repository.ts      # МОДИФИЦ.: фильтр прогресса по userId
  decks/decks.routes.ts              # МОДИФИЦ.: доступ к чужой колоде → 404
  words/words.routes.ts              # МОДИФИЦ.: владение колодой при create/update/delete
  training/progress.repository.ts    # МОДИФИЦ.: getProgress/upsertProgress с userId
  training/training.repository.ts    # МОДИФИЦ.: learnable/due по userId
  training/training.service.ts       # МОДИФИЦ.: прокинуть userId
  training/training.routes.ts        # МОДИФИЦ.: прокинуть req.userId
  stats/studyDays.ts                 # МОДИФИЦ.: recordStudyDay/computeStreak с userId
  stats/stats.service.ts             # МОДИФИЦ.: все запросы по userId
  stats/stats.routes.ts              # МОДИФИЦ.: прокинуть req.userId
client/src/
  auth/authApi.ts                    # МОДИФИЦ.: fetchUsers, register, login(userId, password)
  auth/token.ts                      # МОДИФИЦ.: + wt_user (имя пользователя)
  auth/AuthContext.tsx               # МОДИФИЦ.: user, login(userId,pwd), register(...)
  screens/LoginScreen.tsx            # ЗАМЕНА: выбор профиля → пароль / создание профиля
  screens/ProfileScreen.tsx          # МОДИФИЦ.: имя текущего пользователя
```

**Конвенции:** только `import`; тест-файлы с двумя eslint-disable; НЕ интеграционные тесты; коммит в `main`. `req.userId` — через интерфейс `AuthedRequest` (без `declare global`).

---

### Task 1: Миграция 004 + пароли + users.repository (TDD)

**Files:**
- Create: `server/src/db/migrations/004_users.sql`
- Create: `server/src/auth/password.ts`
- Create: `server/src/auth/users.repository.ts`
- Test: `server/src/auth/__tests__/password.test.ts`
- Test: `server/src/auth/__tests__/users.repository.test.ts`

- [ ] **Step 1: Миграция `004_users.sql`**

```sql
CREATE TABLE IF NOT EXISTS users (
  id            TEXT PRIMARY KEY,
  name          TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

-- Старые личные данные не мигрируем (прода нет): чистим словарь от пользовательских колод
DELETE FROM words WHERE deck_id IN (SELECT id FROM decks WHERE is_builtin = 0);
DELETE FROM decks WHERE is_builtin = 0;
ALTER TABLE decks ADD COLUMN user_id TEXT;

DROP TABLE IF EXISTS progress;
CREATE TABLE progress (
  id               TEXT PRIMARY KEY,
  user_id          TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  word_id          TEXT NOT NULL REFERENCES words(id) ON DELETE CASCADE,
  current_type     INTEGER,
  learned_at       TEXT,
  ease_factor      REAL NOT NULL DEFAULT 2.5,
  interval_days    INTEGER NOT NULL DEFAULT 0,
  next_review_at   TEXT,
  total_reviews    INTEGER NOT NULL DEFAULT 0,
  correct_reviews  INTEGER NOT NULL DEFAULT 0
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_progress_user_word ON progress(user_id, word_id);
CREATE INDEX IF NOT EXISTS idx_progress_review ON progress(next_review_at);

DROP TABLE IF EXISTS study_days;
CREATE TABLE study_days (
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  day     TEXT NOT NULL,
  PRIMARY KEY (user_id, day)
);
```

Проверь `server/src/db/migrate.ts`: миграции применяются по одному разу (таблица применённых), `ALTER TABLE decks ADD COLUMN` не должен выполниться повторно. Если migrate просто гоняет все файлы каждый раз — оберни ALTER в проверку или убедись, что есть журнал миграций (есть — посмотри код).

- [ ] **Step 2: Тест `server/src/auth/__tests__/password.test.ts`**

```ts
/* eslint-disable @typescript-eslint/no-unsafe-call */
/* eslint-disable sonarjs/no-duplicate-string */
import { describe, it, expect } from "vitest";
import { hashPassword, verifyPassword } from "../password.js";

describe("password", () => {
  it("верный пароль проходит проверку", () => {
    const hash = hashPassword("secret123");
    expect(verifyPassword("secret123", hash)).toBe(true);
  });

  it("неверный пароль не проходит", () => {
    const hash = hashPassword("secret123");
    expect(verifyPassword("wrong", hash)).toBe(false);
  });

  it("хэши одного пароля различаются (соль)", () => {
    expect(hashPassword("a")).not.toBe(hashPassword("a"));
  });
});
```

- [ ] **Step 3: Запустить — FAIL. Реализовать `server/src/auth/password.ts`**

```ts
import { randomBytes, scryptSync, timingSafeEqual } from "node:crypto";

const KEY_LEN = 64;

export function hashPassword(password: string): string {
  const salt = randomBytes(16).toString("hex");
  const hash = scryptSync(password, salt, KEY_LEN).toString("hex");
  return `${salt}:${hash}`;
}

export function verifyPassword(password: string, stored: string): boolean {
  const [salt, hash] = stored.split(":");
  if (!salt || !hash) {
    return false;
  }
  const candidate = scryptSync(password, salt, KEY_LEN);
  const expected = Buffer.from(hash, "hex");
  return candidate.length === expected.length && timingSafeEqual(candidate, expected);
}
```

- [ ] **Step 4: Тест `server/src/auth/__tests__/users.repository.test.ts`**

```ts
/* eslint-disable @typescript-eslint/no-unsafe-call */
/* eslint-disable sonarjs/no-duplicate-string */
import { describe, it, expect } from "vitest";
import Database from "better-sqlite3";
import { runMigrations } from "../../db/migrate.js";
import { createUser, getUser, getUserByName, listUsers } from "../users.repository.js";

function freshDb(): Database.Database {
  const db = new Database(":memory:");
  runMigrations(db);
  return db;
}

describe("users.repository", () => {
  it("создаёт пользователя и находит по id и имени", () => {
    const db = freshDb();
    const user = createUser(db, "Женя", "salt:hash");
    expect(getUser(db, user.id)?.name).toBe("Женя");
    expect(getUserByName(db, "Женя")?.id).toBe(user.id);
  });

  it("listUsers отдаёт всех без password_hash", () => {
    const db = freshDb();
    createUser(db, "А", "x:y");
    createUser(db, "Б", "x:y");
    const users = listUsers(db);
    expect(users).toHaveLength(2);
    expect(users[0]).not.toHaveProperty("password_hash");
  });

  it("дубль имени бросает ошибку", () => {
    const db = freshDb();
    createUser(db, "Женя", "x:y");
    expect(() => createUser(db, "Женя", "x:y")).toThrow();
  });
});
```

- [ ] **Step 5: Запустить — FAIL. Реализовать `server/src/auth/users.repository.ts`**

```ts
import { randomUUID } from "node:crypto";
import type Database from "better-sqlite3";

export interface UserRow {
  id: string;
  name: string;
  password_hash: string;
  created_at: string;
}

export interface PublicUser {
  id: string;
  name: string;
}

export function createUser(db: Database.Database, name: string, passwordHash: string): UserRow {
  const id = randomUUID();
  db.prepare("INSERT INTO users (id, name, password_hash) VALUES (?, ?, ?)").run(id, name, passwordHash);
  return getUser(db, id) as UserRow;
}

export function getUser(db: Database.Database, id: string): UserRow | undefined {
  return db.prepare("SELECT * FROM users WHERE id = ?").get(id) as UserRow | undefined;
}

export function getUserByName(db: Database.Database, name: string): UserRow | undefined {
  return db.prepare("SELECT * FROM users WHERE name = ?").get(name) as UserRow | undefined;
}

export function listUsers(db: Database.Database): PublicUser[] {
  return db.prepare("SELECT id, name FROM users ORDER BY created_at ASC").all() as PublicUser[];
}
```

- [ ] **Step 6: Все серверные тесты + tsc.** Старые тесты, использующие `progress`/`study_days` без user_id, СЛОМАЮТСЯ — это ожидаемо, чинятся в Task 3. Если ломаются — запусти только новые: `npx vitest run src/auth/__tests__/password.test.ts src/auth/__tests__/users.repository.test.ts src/db`.

- [ ] **Step 7: Commit**

```bash
git add server/src/db/migrations/004_users.sql server/src/auth/password.ts server/src/auth/users.repository.ts "server/src/auth/__tests__/password.test.ts" "server/src/auth/__tests__/users.repository.test.ts"
git commit -m "Добавить таблицу пользователей, scrypt-пароли и миграцию"
```

---

### Task 2: Auth: register/login/users + middleware (TDD)

**Files:**
- Modify: `server/src/auth/auth.service.ts`
- Modify: `server/src/auth/auth.routes.ts`
- Modify: `server/src/auth/auth.middleware.ts`
- Test: `server/src/auth/__tests__/auth.routes.test.ts` (модифицировать существующий, если есть; иначе создать)

- [ ] **Step 1: `auth.service.ts` — токены с userId**

```ts
import jwt from "jsonwebtoken";

export function signToken(userId: string, secret: string): string {
  return jwt.sign({ sub: userId }, secret, { expiresIn: "30d" });
}

export function verifyToken(token: string, secret: string): string | null {
  try {
    const payload = jwt.verify(token, secret);
    const sub = typeof payload === "object" ? payload.sub : undefined;
    return typeof sub === "string" ? sub : null;
  } catch {
    return null;
  }
}
```

Старые `checkPassword` оставить — `familyCode` сверяется им же (timingSafeEqual).

- [ ] **Step 2: `auth.routes.ts`**

```ts
import { Router } from "express";
import type Database from "better-sqlite3";
import type { AppConfig } from "../config.js";
import { checkPassword, signToken } from "./auth.service.js";
import { hashPassword, verifyPassword } from "./password.js";
import { createUser, getUser, getUserByName, listUsers } from "./users.repository.js";

export function createAuthRouter(config: AppConfig, db: Database.Database): Router {
  const router = Router();

  router.get("/users", (_req, res) => {
    res.json({ users: listUsers(db) });
  });

  router.post("/register", (req, res) => {
    const body = req.body ?? {};
    const name = typeof body.name === "string" ? body.name.trim() : "";
    const password = typeof body.password === "string" ? body.password : "";
    const familyCode = typeof body.familyCode === "string" ? body.familyCode : "";

    if (!checkPassword(familyCode, config.appPassword)) {
      res.status(403).json({ error: "Неверный код семьи" });
      return;
    }
    if (!name || password.length < 4) {
      res.status(400).json({ error: "Нужны имя и пароль (от 4 символов)" });
      return;
    }
    if (getUserByName(db, name)) {
      res.status(409).json({ error: "Имя уже занято" });
      return;
    }

    const user = createUser(db, name, hashPassword(password));
    res.status(201).json({ token: signToken(user.id, config.jwtSecret), user: { id: user.id, name: user.name } });
  });

  router.post("/login", (req, res) => {
    const body = req.body ?? {};
    const userId = typeof body.userId === "string" ? body.userId : "";
    const password = typeof body.password === "string" ? body.password : "";

    const user = getUser(db, userId);
    if (!user || !verifyPassword(password, user.password_hash)) {
      res.status(401).json({ error: "Неверный пароль" });
      return;
    }

    res.json({ token: signToken(user.id, config.jwtSecret), user: { id: user.id, name: user.name } });
  });

  return router;
}
```

В `app.ts` вызов меняется на `createAuthRouter(config, db)`.

- [ ] **Step 3: `auth.middleware.ts` — userId в req**

```ts
import type { NextFunction, Request, Response } from "express";
import type Database from "better-sqlite3";
import { verifyToken } from "./auth.service.js";
import { getUser } from "./users.repository.js";

export interface AuthedRequest extends Request {
  userId?: string;
}

export function createAuthMiddleware(secret: string, db: Database.Database) {
  return (req: AuthedRequest, res: Response, next: NextFunction): void => {
    const header = req.headers.authorization ?? "";
    const token = header.startsWith("Bearer ") ? header.slice(7) : "";
    const userId = token ? verifyToken(token, secret) : null;

    if (!userId || !getUser(db, userId)) {
      res.status(401).json({ error: "Не авторизован" });
      return;
    }
    req.userId = userId;
    next();
  };
}
```

В `app.ts`: `createAuthMiddleware(config.jwtSecret, db)`.

- [ ] **Step 4: Тесты роутов** — supertest НЕ используется в проекте (проверь существующие `__tests__` роутов и следуй их подходу; если роуты тестов не имеют — тестируй на уровне сервисов: register-логика через прямые вызовы репозиториев + password, и `verifyToken(signToken(...))` round-trip):

```ts
/* eslint-disable @typescript-eslint/no-unsafe-call */
/* eslint-disable sonarjs/no-duplicate-string */
import { describe, it, expect } from "vitest";
import { signToken, verifyToken } from "../auth.service.js";

describe("auth.service токены", () => {
  it("verifyToken возвращает userId из токена", () => {
    const token = signToken("user-1", "s3cret");
    expect(verifyToken(token, "s3cret")).toBe("user-1");
  });

  it("чужой секрет — null", () => {
    const token = signToken("user-1", "s3cret");
    expect(verifyToken(token, "other")).toBeNull();
  });
});
```

(Положи в существующий файл тестов auth-сервиса, если есть, иначе создай `server/src/auth/__tests__/auth.service.test.ts`. Существующие тесты на `signToken()` без аргумента userId почини под новую сигнатуру.)

- [ ] **Step 5: Прогон новых auth-тестов + tsc. Commit**

```bash
git add server/src/auth server/src/app.ts
git commit -m "Перевести авторизацию на пользователей: register, login, users"
```

---

### Task 3: Скоупинг данных по userId (сервер целиком + починка тестов)

**Files (Modify):**
- `server/src/decks/decks.repository.ts`, `deckStats.repository.ts`, `decks.routes.ts`
- `server/src/words/words.routes.ts`
- `server/src/training/progress.repository.ts`, `training.repository.ts`, `training.service.ts`, `training.routes.ts`
- `server/src/stats/studyDays.ts`, `stats.service.ts`, `stats.routes.ts`
- Все затронутые `__tests__` (создают тестового юзера и передают userId)

Сигнатуры (точно такие):

```ts
// decks.repository
listDecks(db, userId: string): DeckRow[]            // WHERE is_builtin = 1 OR user_id = ?
createDeck(db, name: string, userId: string): DeckRow  // INSERT ... user_id
// getDeck без изменений; добавить:
canAccessDeck(deck: DeckRow, userId: string): boolean  // is_builtin === 1 || deck.user_id === userId

// deckStats.repository
listDecksWithStats(db, userId: string): DeckWithStats[]
//   total как раньше; learned: JOIN progress p ... AND p.user_id = ?
//   WHERE d.is_builtin = 1 OR d.user_id = ?
listWordsWithProgress(db, deckId: string, userId: string)
//   LEFT JOIN progress p ON p.word_id = w.id AND p.user_id = ?

// progress.repository
getProgress(db, userId: string, wordId: string)
upsertProgress(db, userId: string, wordId: string, fields)   // INSERT с user_id; WHERE user_id AND word_id

// training.repository
listLearnableWords(db, userId: string, limit: number)
//   JOIN decks d ON d.id = w.deck_id
//   LEFT JOIN progress p ON p.word_id = w.id AND p.user_id = ?
//   WHERE (d.is_builtin = 1 OR d.user_id = ?) AND (p.id IS NULL OR p.learned_at IS NULL)
listDueReviews(db, userId: string, nowIso: string)           // + p.user_id = ?

// training.service
getTodayTraining(db, userId, now, batchSize?)
recordLearningStep(db, userId, wordId, currentType)
markLearned(db, userId, wordId, now)
recordReview(db, userId, wordId, correct, now)

// stats
recordStudyDay(db, userId, day)        // INSERT OR IGNORE (user_id, day)
computeStreak(db, userId, now)         // SELECT day WHERE user_id = ?
getStats(db, userId, now)              // все COUNT с user_id; week по user_id; decks = listDecksWithStats(db, userId)
```

Роуты: тип запроса `AuthedRequest`, `const userId = req.userId as string` (middleware гарантирует). Правила доступа:
- `GET /api/decks/:id/words`: `!deck || !canAccessDeck(deck, userId)` → 404
- `POST /api/words`: колода не найдена/чужая → 404; builtin → 403 (как раньше)
- `PUT/DELETE /api/words/:id`: слово → его колода; чужая → 404; builtin → 403
- training `/result`: перед записью проверить, что слово доступно (его колода builtin или своя), иначе 404

Починка тестов: в каждом затронутом тесте создать юзера хелпером:

```ts
import { createUser } from "../../auth/users.repository.js"; // путь подстрой
const user = createUser(db, "Тестер", "salt:hash");
// и передавать user.id во все вызовы
```

- [ ] **Step 1:** Репозитории + сервисы (с правкой их тестов, по одному модулю: запusti тесты модуля после правки)
- [ ] **Step 2:** Роуты (decks, words, training, stats) + `app.ts`
- [ ] **Step 3:** Полный `npm test` PASS + `npx tsc --noEmit` чисто
- [ ] **Step 4: Commit**

```bash
git add server/src
git commit -m "Скоупить колоды, прогресс и статистику по пользователю"
```

---

### Task 4: Клиент: auth-слой + экран выбора профиля

**Files:**
- Modify: `client/src/auth/authApi.ts`, `client/src/auth/token.ts`, `client/src/auth/AuthContext.tsx`
- Replace: `client/src/screens/LoginScreen.tsx`
- Modify: `client/src/screens/ProfileScreen.tsx`
- Tests: `client/src/auth/__tests__/authApi.test.ts` (модифиц.), `client/src/screens/__tests__/LoginScreen.test.tsx` (заменить), `client/src/screens/__tests__/ProfileScreen.test.tsx` (поправить мок useAuth)

- [ ] **Step 1: `token.ts`** — добавить хранение пользователя:

```ts
const TOKEN_KEY = "wt_token";
const USER_KEY = "wt_user";

export interface StoredUser {
  id: string;
  name: string;
}

export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY);
}

export function saveToken(token: string): void {
  localStorage.setItem(TOKEN_KEY, token);
}

export function clearToken(): void {
  localStorage.removeItem(TOKEN_KEY);
}

export function getStoredUser(): StoredUser | null {
  const raw = localStorage.getItem(USER_KEY);
  if (!raw) return null;
  try {
    return JSON.parse(raw) as StoredUser;
  } catch {
    return null;
  }
}

export function saveStoredUser(user: StoredUser): void {
  localStorage.setItem(USER_KEY, JSON.stringify(user));
}

export function clearStoredUser(): void {
  localStorage.removeItem(USER_KEY);
}
```

- [ ] **Step 2: `authApi.ts`**

```ts
import { apiRequest } from "../api/http";

export interface AuthUser {
  id: string;
  name: string;
}

export interface AuthResult {
  token: string;
  user: AuthUser;
}

export async function fetchUsers(fetchFn: typeof fetch = fetch): Promise<AuthUser[]> {
  const data = await apiRequest<{ users: AuthUser[] }>("/api/auth/users", {}, fetchFn);
  return data.users;
}

export async function login(
  userId: string,
  password: string,
  fetchFn: typeof fetch = fetch,
): Promise<AuthResult> {
  return apiRequest<AuthResult>("/api/auth/login", { method: "POST", body: { userId, password } }, fetchFn);
}

export async function register(
  name: string,
  password: string,
  familyCode: string,
  fetchFn: typeof fetch = fetch,
): Promise<AuthResult> {
  return apiRequest<AuthResult>(
    "/api/auth/register",
    { method: "POST", body: { name, password, familyCode } },
    fetchFn,
  );
}
```

Тест `authApi.test.ts` переписать: login шлёт `{userId, password}` и возвращает `{token, user}`; register шлёт `{name, password, familyCode}`; fetchUsers возвращает список.

- [ ] **Step 3: `AuthContext.tsx`**

```tsx
import { createContext, useContext, useState, type ReactNode } from "react";
import {
  clearStoredUser,
  clearToken,
  getStoredUser,
  getToken,
  saveStoredUser,
  saveToken,
  type StoredUser,
} from "./token";
import { login as loginApi, register as registerApi } from "./authApi";

interface AuthValue {
  isAuthenticated: boolean;
  user: StoredUser | null;
  login: (userId: string, password: string) => Promise<void>;
  register: (name: string, password: string, familyCode: string) => Promise<void>;
  logout: () => void;
}

const AuthContext = createContext<AuthValue | null>(null);

export function AuthProvider({ children }: { children: ReactNode }) {
  const [token, setToken] = useState<string | null>(() => getToken());
  const [user, setUser] = useState<StoredUser | null>(() => getStoredUser());

  const apply = (result: { token: string; user: StoredUser }): void => {
    saveToken(result.token);
    saveStoredUser(result.user);
    setToken(result.token);
    setUser(result.user);
  };

  const login = async (userId: string, password: string): Promise<void> => {
    apply(await loginApi(userId, password));
  };

  const register = async (name: string, password: string, familyCode: string): Promise<void> => {
    apply(await registerApi(name, password, familyCode));
  };

  const logout = (): void => {
    clearToken();
    clearStoredUser();
    setToken(null);
    setUser(null);
  };

  return (
    <AuthContext.Provider value={{ isAuthenticated: token !== null, user, login, register, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth(): AuthValue {
  const ctx = useContext(AuthContext);
  if (!ctx) {
    throw new Error("useAuth вызван вне AuthProvider");
  }
  return ctx;
}
```

Существующий `AuthContext.test.tsx` поправить под новые сигнатуры (login с двумя аргументами, мок authApi возвращает `{token, user}`).

- [ ] **Step 4: `LoginScreen.tsx`** — три вида: `pick` (сетка профилей + «Создать профиль»), `password` (выбранный профиль + поле пароля), `create` (имя/пароль/код семьи). Дизайн в стиле текущего LoginScreen (карточка по центру, Card/Button/Icon). Аватар — круг с `hsl(hue, 70%, 85%)` от `hueFromString(name)` и первой буквой имени. Состояния: загрузка списка (`fetchUsers` через `useAsync`), ошибки логина/регистрации под формой (`role="alert"`). Если профилей нет — сразу подсказка создать первый.

Тест `LoginScreen.test.tsx` (заменить):

```tsx
/* eslint-disable @typescript-eslint/no-unsafe-call */
/* eslint-disable sonarjs/no-duplicate-string */
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { LoginScreen } from "../LoginScreen";

const fetchUsersMock = vi.fn();
vi.mock("../../auth/authApi", () => ({
  fetchUsers: () => fetchUsersMock(),
  login: vi.fn(),
  register: vi.fn(),
}));

const loginMock = vi.fn();
const registerMock = vi.fn();
vi.mock("../../auth/AuthContext", () => ({
  useAuth: () => ({ login: loginMock, register: registerMock, isAuthenticated: false, user: null, logout: vi.fn() }),
}));

beforeEach(() => {
  fetchUsersMock.mockReset();
  loginMock.mockReset();
  registerMock.mockReset();
});

describe("LoginScreen", () => {
  it("показывает профили и переходит к паролю", async () => {
    fetchUsersMock.mockResolvedValue([{ id: "u1", name: "Женя" }]);
    const user = userEvent.setup();
    render(<LoginScreen />);
    await waitFor(() => expect(screen.getByText("Женя")).toBeInTheDocument());

    await user.click(screen.getByText("Женя"));
    await user.type(screen.getByLabelText("Пароль"), "1234");
    await user.click(screen.getByRole("button", { name: "Войти" }));
    expect(loginMock).toHaveBeenCalledWith("u1", "1234");
  });

  it("создаёт профиль", async () => {
    fetchUsersMock.mockResolvedValue([]);
    const user = userEvent.setup();
    render(<LoginScreen />);
    await waitFor(() => expect(screen.getByRole("button", { name: /Создать профиль/ })).toBeInTheDocument());

    await user.click(screen.getByRole("button", { name: /Создать профиль/ }));
    await user.type(screen.getByLabelText("Имя"), "Маша");
    await user.type(screen.getByLabelText("Пароль"), "1234");
    await user.type(screen.getByLabelText("Код семьи"), "family");
    await user.click(screen.getByRole("button", { name: "Создать" }));
    expect(registerMock).toHaveBeenCalledWith("Маша", "1234", "family");
  });
});
```

- [ ] **Step 5: `ProfileScreen.tsx`** — взять `user` из `useAuth()`, в карточке профиля показывать `user?.name ?? "Профиль"` и первую букву имени в аватаре. Тест ProfileScreen поправить: мок `useAuth` теперь возвращает `{ logout, user: { id: "u1", name: "Женя" } }`, проверить что имя отображается.

- [ ] **Step 6: Полный прогон клиента + tsc + build. Commit**

```bash
git add client/src
git commit -m "Экран выбора профиля и вход по личному паролю"
```

---

### Task 5: Ручная проверка в браузере

- [ ] Перезапустить бэкенд (миграция 004 применится), открыть фронт
- [ ] Экран выбора профилей пуст → «Создать профиль»: имя + пароль + код семьи (`local-dev`) → автологин, дашборд
- [ ] Выйти → профиль в списке → вход по паролю
- [ ] Создать второго пользователя, убедиться: колода первого не видна второму; встроенные видны обоим; прогресс раздельный (пройти по 1 слову у каждого)
- [ ] Скриншоты экранов профилей
- [ ] Если правки — закоммитить

---

## Self-Review

**Spec coverage:** users + scrypt (T1) ✓; register/login/users + familyCode (T2) ✓; изоляция decks/progress/stats/study_days (T3) ✓; экран профилей + создание + ProfileScreen (T4) ✓; чистка старых данных миграцией (T1 SQL) ✓; «чужая колода → 404» (T3 правила) ✓.

**Type consistency:** `AuthedRequest.userId` (T2) используется в T3 роутах; `signToken(userId, secret)` (T2) — в register/login; `StoredUser`/`AuthUser` `{id, name}` согласованы между token.ts/authApi/AuthContext; `login(userId, password)` совпадает в authApi, AuthContext и тесте LoginScreen.

**Placeholder scan:** чисто; для LoginScreen дан контракт поведения + тест вместо полного JSX (дизайн повторяет существующий стиль, исполнителю даны компоненты).
